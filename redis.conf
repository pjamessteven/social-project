# RDB snapshots
save 3600 1
save 300 100
save 60 10000

# AOF persistence
appendonly yes
appendfsync everysec

# Memory settings
maxmemory 1536mb
maxmemory-policy allkeys-lru

# Data directory (mounted volume)
dir /data
